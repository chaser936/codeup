#include <iostream>

using namespace std;

int main() {
    // Write C++ code here
    float a;
    
    cin >> a;
    cin.setf(ios::showpoint);
    cout << fixed;
    cout.precision(2);
    cout << a;

    return 0;
}
